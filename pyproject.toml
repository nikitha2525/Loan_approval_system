[project]
name = "loan-approval-system"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "numpy",
    "pandas",
    "scikit-learn",
]

[tool.vercel]
entrypoint = "app:app"
